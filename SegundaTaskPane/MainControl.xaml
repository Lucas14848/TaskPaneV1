<UserControl x:Class="SegundaTaskPane.MainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">

    <UserControl.Resources>
        <Style x:Key="TechButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D31"/>
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="4" BorderBrush="#3F3F46" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E42"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#62AEEF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#1B1B1C" Offset="0"/>
                <GradientStop Color="#252526" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <StackPanel Margin="15">
            <TextBlock Text="SW COMMAND CENTER" Foreground="#0078D4" FontSize="14" 
                       FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="8" Color="#0078D4" Opacity="0.5" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>

            <Button Content="NOVA PEÇA" Click="BtnNovo_Click" Style="{StaticResource TechButtonStyle}"/>
            <Button Content="ESBOÇO FRONTAL" Click="BtnEsboco_Click" Style="{StaticResource TechButtonStyle}"/>
            <Button Content="CRIAR CUBO 100mm" Click="BtnCriarCubo_Click" Style="{StaticResource TechButtonStyle}"/>

            <Separator Background="#3F3F46" Margin="0,20"/>
            <TextBlock Text="STATUS: ACTIVE" Foreground="#2D7D46" FontSize="10" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>